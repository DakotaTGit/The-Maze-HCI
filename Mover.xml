<Defaults>
    <Button 
        width="56" height="56" border-radius="8" 
        background-color="white" font-size="22" 
        textColor="rgb(0,0,0)" 
        boxShadow="0 1px 0 #999" 
        activeTransform="translateY(1)"
    />

    <Panel 
        padding="18" border-radius="18" 
        background-color="#e0e0e0" 
        boxShadow="0 1px 0 #aaa"
    />

    <Text 
        font-family="system-ui,Segoe UI,Roboto,Arial" 
        color="#666" font-size="14"
    />
</Defaults>

<Panel color="#e6e6e6" padding="18">
    <VerticalLayout spacing="8">

        <Panel color="grey" height="40">
            <Text text="The Maze: Viewer"/>
        </Panel>

        <Panel color="#dcdcdc" height="220">
            <!-- Maze -->
        </Panel>

        <HorizontalLayout spacing="12">
            <Panel flex="1" color="#dcdcdc" padding="12">
                <VerticalLayout spacing="6">
                    <Text text="Symbols (random 6 per game)"/>
                    <Panel name="symbolGrid"/>
                    <HorizontalLayout spacing="8">
                        <Button name="reshuffle" text="New Set"/>
                        <Button name="clearSymbols" text="Clear Sent"/>
                    </HorizontalLayout>
                </VerticalLayout>
            </Panel>

            <Panel width="260" color="#dcdcdc" padding="12">
                <VerticalLayout spacing="6">
                    <Text text="Timer: 00:00" name="timer"/>
                    <Text text="Deaths: 0" name="deaths"/>
                    <Text text="Comm: BroadcastChannel"/>
                </VerticalLayout>
            </Panel>
        </HorizontalLayout>

        <Text text="Tip: Click a symbol to send it to the Mover."/>
    </VerticalLayout>
</Panel>
